# Public Render API URL — baked into the production bundle (not a secret).
# Override in Vercel → Settings → Environment Variables if your API host differs.
VITE_API_URL=https://rolepbp-api.onrender.com
